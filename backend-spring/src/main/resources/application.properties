spring.application.name=MedicalIoT
server.port=8080
server.address=0.0.0.0

# Enable Java 21 Virtual Threads
spring.threads.virtual.enabled=true

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/medical_iot_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=malcolm
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Security (Temporary for dev)
spring.security.user.name=admin
spring.security.user.password=password

analytics.url=http://localhost:4242/analyze
